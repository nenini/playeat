<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nyamnyam.coach.quest.repository.QuestTemplateRepository">

    <resultMap id="QuestTemplateMap" type="com.nyamnyam.coach.quest.entity.QuestTemplate">
        <id property="templateId" column="template_id"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="questType" column="quest_type"/>
        <result property="conditionCategory" column="condition_category"/>
        <result property="metricType" column="metric_type"/>
        <result property="comparisonType" column="comparison_type"/>
        <result property="aggregationType" column="aggregation_type"/>
        <result property="evaluationScope" column="evaluation_scope"/>
        <result property="thresholdValue" column="threshold_value"/>
        <result property="thresholdMinValue" column="threshold_min_value"/>
        <result property="thresholdMaxValue" column="threshold_max_value"/>
        <result property="thresholdUnit" column="threshold_unit"/>
        <result property="targetValue" column="target_value"/>
        <result property="unit" column="unit"/>
        <result property="damage" column="damage"/>
        <result property="rewardExp" column="reward_exp"/>
        <result property="rewardCoin" column="reward_coin"/>
        <result property="difficulty" column="difficulty"/>
        <result property="active" column="active"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="QuestTemplateColumns">
        template_id,
        title,
        description,
        quest_type,
        condition_category,
        metric_type,
        comparison_type,
        aggregation_type,
        evaluation_scope,
        threshold_value,
        threshold_min_value,
        threshold_max_value,
        threshold_unit,
        target_value,
        unit,
        damage,
        reward_exp,
        reward_coin,
        difficulty,
        active,
        sort_order,
        created_at,
        updated_at
    </sql>

    <select id="findActiveTemplates" resultMap="QuestTemplateMap">
        SELECT <include refid="QuestTemplateColumns"/>
        FROM quest_templates
        WHERE active = TRUE
        ORDER BY sort_order ASC, template_id ASC
    </select>

    <select id="findActiveTemplatesByDifficulty" resultMap="QuestTemplateMap">
        SELECT <include refid="QuestTemplateColumns"/>
        FROM quest_templates
        WHERE active = TRUE
          AND difficulty = #{difficulty}
        ORDER BY sort_order ASC, template_id ASC
    </select>

    <select id="findTemplateById" resultMap="QuestTemplateMap">
        SELECT <include refid="QuestTemplateColumns"/>
        FROM quest_templates
        WHERE template_id = #{templateId}
        LIMIT 1
    </select>

    <select id="findDefaultTemplate" resultMap="QuestTemplateMap">
        SELECT <include refid="QuestTemplateColumns"/>
        FROM quest_templates
        WHERE active = TRUE
        ORDER BY sort_order ASC, template_id ASC
        LIMIT 1
    </select>
</mapper>
