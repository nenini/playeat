spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nyamnyam?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: nyamnyam
    password: nyamnyam1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  data:
    redis:
      host: localhost
      port: 6379
      password: redis1234

jwt:
  secret: your-base64-jwt-secret
  access-token-expiration: 1800000
  refresh-token-expiration: 604800000

gms:
  enabled: false
  base-url: https://gms.ssafy.io/gmsapi/api.openai.com/v1
  model: gpt-5-mini
  api-key: ${GMS_KEY:}
  timeout-ms: 60000
