# FrontEnd

```powershell
npm install
npm run dev
npm run build
```

The frontend calls the backend through `VITE_API_BASE_URL` (`/api` by default).
During local development, Vite proxies `/api` to `http://localhost:8080`.
